#taking the input of numerator and denominator
a1=int(input('enter the numerator of 1st fraction'))
b1=int(input('enter the denominator of 1st fraction'))
a2=int(input('enter the numerator of 2nd fraction'))
b2=int(input('enter the denominator of 2nd fraction'))
#applying logic of adding the fractions 
x=float(a1/b1)
y=float(a2/b2)
r=float(x+y)
print(r)